#pragma once

#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include "classes.h"
using namespace std;

vector<TIntNumber*> AddNewBinaryNumber(vector<TIntNumber*>);

vector<TIntNumber*> AddNewOctalNumber(vector<TIntNumber*>);

int findSmallestDecimal(vector<TIntNumber*>);